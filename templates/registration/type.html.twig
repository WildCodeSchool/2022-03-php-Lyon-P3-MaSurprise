{% extends 'base.html.twig' %}

{% block title %}Créer un compte{% endblock %}

{% block body %}
    <h1 class="text-primary text-center">Créer un compte</h1>

<div class="d-flex justify-content-around">
    <a href="{{ path('app_register_baker')}}" class="btn btn-primary mt-4">Compte pâtissier</a>
    <a href="{{ path('app_register_user')}}" class="btn btn-primary mt-4">Compte client</a>
</div>
{% endblock %}
